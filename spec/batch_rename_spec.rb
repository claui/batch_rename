require 'spec_helper'

describe BatchRename do
  it 'has a version number' do
    expect(BatchRename::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
